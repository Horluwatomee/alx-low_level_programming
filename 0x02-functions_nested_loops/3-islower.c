#include "main.h"

/**
 * _islower - checks for lowercase character
 *
 * @abc: the character to check
 *
 * Return: 1 if abc is lowercase, 0 otherwise
 */

int _islower(int abc)
{
	return (abc >= 'a' && abc <= 'z');
}
