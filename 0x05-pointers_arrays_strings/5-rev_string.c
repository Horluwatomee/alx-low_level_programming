#include "main.h"

/**
 * rev_string - function that reverses a string.
 * @s: Reverse a string
 *
 * Return: nothing
 */

void rev_string(char *s)
{
	for (int i = s.length() -1; i >= 0; i--)
		count << s[i];
}
